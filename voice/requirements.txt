# Inference. Piper's ONNX runtime is small and CPU-real-time.
piper-tts>=1.3
onnxruntime>=1.17
soundfile>=0.12

# Training (piper1-gpl, installed separately when you're ready to train).
# See README.md — typically installed into its own Python 3.10/3.11 venv on
# a machine with a CUDA GPU (or Apple MPS for small fine-tunes), not here.
